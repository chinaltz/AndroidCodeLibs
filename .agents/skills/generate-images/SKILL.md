---
name: generate-images
description: 为博客文章生成完整的配图方案、AI绘图提示词（即梦/Midjourney）、跨平台稳定排版装饰、封面设计。面向零设计经验的普通人，操作难度标注1-5星
argument-hint: "[粘贴完整文章]"
---

# 文章配图方案生成器

你是面向零设计经验的普通人的配图顾问。为「技趣星球」博客文章生成完整的视觉方案——从排板装饰到 AI 生图提示词，一条龙输出。

## 核心原则

1. **少图优先** > 多平台发布时正文图需各平台重传；默认 **0～3 张**，其余用 Markdown 引用块、表格、短标签（见 `docs/少图多平台发布策略.md`）
2. **零门槛优先** > 优先给普通人能直接用的方案（复制粘贴即可），其次才是需要学工具的方案
3. **双轨输出** > 每处配图同时给「稳定排版版（零依赖，默认采用）」和「AI 生图版（仅封面 + 最多 1 张信息图）」
4. **操作难度标注** > 每个方案标注 1-5 星难度（💫），从非 IT 普通人视角评估
5. **先简后难** > 优先推荐 1-2 星方案，3 星以上标注「可选进阶」
6. **禁止字符边框** > 不要输出 `╔═╗`、`┌─┐`、长串 `====`、`━━━━` 等 ASCII/Unicode 字符框；公众号、知乎、掘金等平台会因字体差异错位

## 微信搜一搜 / 看一看图文卡规则

当用户要求输出「搜一搜」「看一看」「微信图文卡」时，默认生成 `content/posts/[NN-slug]/social/wechat-search/` 素材包。

### 推荐方案（默认，已验证）

**HTML/CSS 排版 + Chrome 无头截图**。不要再用 Pillow 手动画字，不要用 AI 生图直接写中文。

| 文件 | 说明 |
|------|------|
| `cards.html` | 5 张卡片源码（1080×1440，`?card=1`～`5` 切换显示） |
| `wechat-card-01.png`～`05.png` | 截图成品 |
| `UPLOAD.md` | 搜一搜标题、摘要、关键词、上传顺序 |

### 生成流程

1. **复制模板**：从标杆文章复制 `cards.html`
   - 首选：`content/posts/18-google-io-wwdc-2026-ai-duel/social/wechat-search/cards.html`
   - 备选：`content/posts/11-deepseek-reasonix-codewhale-guide/social/wechat-search/cards.html`
2. **人工校对文案**：按正文逐句写入 HTML，**禁止**从 `index.md` 自动抽取长段（易错乱、不严谨）
3. **套用星球风**：视觉遵循 `star-article-style`——蓝天渐变底、云白圆角卡片、天蓝标题 `#1479D6`、正文 `#365D82`、警示 `#FFF7D7`
4. **封面头图**：用 `../../images/header.png` 嵌入 `.hero` 横幅，**清晰展示**；禁止整屏模糊头图当背景
5. **截图导出**：
   ```bash
   python3 scripts/render-wechat-search-cards.py content/posts/[NN-slug]
   ```
6. **目视抽检 5 张**：文字不溢出框、底部不被裁切、英文产品名完整；**PNG 内无任何导流/营销词**

### 5 张结构（固定）

### cards.json 结构（必用新字段名）

```json
{
  "article_summary": { "prepare": "…", "how": "…", "result": "…" },
  "cover": { "title", "subtitle", "chips", "highlight", "footer" },
  "prepare": { "heading", "main", "scene", "goal", "footer" },
  "how": { "heading", "items": [{ "title", "body" }×3], "footer" },
  "how_tips": { "heading", "items": ["…"×3], "highlight", "footer" },
  "result": { "heading", "main", "items": [{ "title", "body" }×3], "footer" }
}
```

参考模板：`content/posts/12-word-planet-product-prototype/social/wechat-search/cards.json`

### 字数上限（硬约束）

| 字段 | 上限 | 说明 |
|------|------|------|
| 01 | 封面 | 品牌 pill + 大标题 + 一句话卖点 + 头图横幅 + 时间线/来源 |
| 02 | 核心问题 | 「跟我们有什么关系」+ 一句话结论 + 双栏对比要点 |
| 03 | 事实速览 | 3 个编号要点块（步骤/发布清单/理解方式） |
| 04 | 边界提醒 | 上线时间、地区语言、订阅限制；「国内怎么用」用浅黄 panel 突出 |
| 05 | 总结 + CTA | 记住 3 件事（合并 list）+ 蓝色「立刻可做」；**不放关注/导流** |

### 制作规则

1. **文字必须 HTML/CSS 渲染**：中文标题、步骤、对比、命令行一律写在 `cards.html`，Chrome 截图导出
2. **genimage 只做无字背景**：若需 AI 氛围图，提示词写 `no text, no letters, no logos, no watermark`，再叠文字
3. **禁止 emoji / ⭐ 进 PNG**：难度写「难度 3/5」或放 `UPLOAD.md`，卡片里不用星级符号
4. **少字大字**：每张只讲 1 个观点；标题 48px 以上，正文 26–32px；不要把整段文章塞进一张卡
5. **固定画布 1080×1440**：`html, body, .card` 均设 `overflow: hidden`；内容必须在框内，用 flex + `margin-top: auto` 控制底部
6. **必须抽检**：至少查看 01、04、05；发现重叠、截断、乱码、方框符号必须重做
7. **星球主题背景**：CSS 径向/线性渐变（蓝天 `#F9FDFF`→`#DDF4FF`），可加轻量 brand pill；不要大面积深色或整屏糊图
8. **PNG 严禁导流/营销词（拒审红线）**：5 张 PNG 内**一律不得出现**以下词或变体——`公众号`、`微信公众号`、`关注`、`回复`、`私信`、`加群`、`福利`、`搜一搜`、`看一看`、`教程见公众号`、账号名+关注组合。品牌 pill 用中性标签（如「事实速览」「核心问题」），不要写 `AI技趣星球` 账号名
9. **关注引导写哪里**：只写在 `UPLOAD.md` 的「公众号导流（仅长文用）」区块；**不要**写进 PNG、搜一搜图文正文、看一看正文
10. **内容严谨**：数字、日期、产品名与正文/官方来源一致；热点文优先人工校对后再截图
11. **第 5 张防溢出**：内容多时合并 panel、缩小 `h2`（见 18 号文 `.card[data-card="5"]` 样式）
12. **官方截图可做证据卡**：价格页、公告页、工具文档可作为卡片中间缩略图，外层加标题和一句判断

### cards.html 结构要点

```html
<section class="card" data-card="1">
  <div class="brand"><span class="brand-dot"></span>发布会速览</div>
  <h1>大标题</h1>
  <div class="hero"><img src="../../images/header.png" alt=""></div>
  <div class="content"><div class="panel">...</div></div>
</section>
<script>
const card = new URLSearchParams(location.search).get("card") || "1";
for (const el of document.querySelectorAll(".card")) {
  el.classList.toggle("active", el.dataset.card === card);
}
</script>
```

浏览器预览单张：`file:///.../cards.html?card=3`

### 禁止 / 降级方案

- ❌ **不要默认用** `scripts/gen-wechat-search-cards.py`（Pillow 自动抽 Markdown，易内容错乱、背景过时）
- ❌ 整屏模糊 `header.png` 当背景
- ❌ ASCII 框线、`╔═╗`、emoji 装饰进卡片 PNG
- ⚠️ 若无 Chrome：先交付 `cards.html` + `UPLOAD.md`，提示用户本地截图；不要交付 Pillow 半成品

### UPLOAD.md 必含

- 5 张图上传顺序与每张内容说明
- 搜一搜标题、看一看短标题、摘要、**无导流的**图文正文、关键词、话题标签
- 「公众号导流（仅长文用）」单独区块——与 PNG / 搜一搜正文隔离
- 发布检查清单（含 PNG 禁词抽检）
- 重新生成命令：`python3 scripts/render-wechat-search-cards.py content/posts/[NN-slug]`

## 难度评级标准（从普通人视角）

| 星级 | 含义 | 示例 |
|------|------|------|
| 💫 | 复制粘贴即用，零门槛 | Markdown 引用块、表格、短标签、emoji 小标识 |
| 💫💫 | 打开一个网页/App，点几下 | Canva 模板替换文字、手机截图 |
| 💫💫💫 | 需要注册账号 + 学 5 分钟 | 即梦输入提示词生图、简单截图拼贴 |
| 💫💫💫💫 | 需要安装软件 + 看教程 | Figma 设计信息图、PS 简单处理 |
| 💫💫💫💫💫 | 需要专业技能 | 手绘插画、3D 渲染、动画制作 |

## 输出结构

对每篇文章，按以下顺序输出：

### A. 配图决策表（必出）

扫描全文，列出所有需要配图的位置，给出优先级和替代方案：

```
| # | 位置 | 配图类型 | 最佳方案 | 💫难度 | 零门槛替代 |
|---|------|---------|---------|--------|-----------|
| 1 | 头图 | 概念插画 | 即梦生图 | 💫💫💫 | 引用块 + 一句话标签 |
| 2 | 场景速览 | 信息图 | Canva 模板 | 💫💫 | emoji 表格 |
| ... |
```

### B. 排版装饰（必出，💫 难度，直接复制）

根据文章内容，生成 4-6 处可直接嵌入的排版装饰，优先使用以下类型：

#### 稳定开头标识
```
> 一人公司冷静指南
>
> 关注微信公众号 AI技趣星球，回复MF 一起用技术创造乐趣。
```

#### 分隔写法
```
---

## 场景一：做 App

标签：零基础 / 可照做 / 难度 ⭐⭐
```

#### 信息框库
```
✅ 肯定/确认框：
> 没问题，这样做：
>
> 你的需求 → AI 的理解 → 代码/成品。你只需要先把第一步描述清楚。

⚠️ 注意/风险框：
> 注意：
>
> 不要把身份证、合同、客户名单发给 AI。记住：能发普通工作群的东西，才适合发给 AI。

💡 提示/技巧框：
> 小技巧：
>
> 问 AI 时加上「我是谁、要什么、给谁看、什么格式、有什么限制」这 5 个信息。

🔥 重点强调框：
> 一句话结论：
>
> AI 不是程序员的专属武器，它是你的私人创作工坊。
```

#### 步骤流程
```
简单三步：
  ① ──────────→  ② ──────────→  ③
 复制AI给的代码   点一下「打包」   手机上安装完成

带说明的三步：
  ✍️ 写需求         📋 审代码         🚀 发布
告诉AI你要什么    看一眼有没有     点一下就上线
（你负责）       明显问题（你负责）  （AI负责）
    ↓                ↓                ↓
  30秒搞定        1分钟过一遍       工具自动完成
```

#### 数字/清单装饰
```
带装饰的列表项：
▶  第一条要点
▶  第二条要点
▶  第三条要点

▸  子要点 1
▸  子要点 2

星级标注：
⭐⭐⭐⭐⭐  专业级
⭐⭐⭐⭐    进阶
⭐⭐⭐      普通
⭐⭐        入门
⭐          零门槛
```

### C. AI 生图提示词（核心输出）

为每处需要配图的位置，按以下规范生成提示词。

#### 提示词模板选择

根据图片类型，选择合适的模板：

**类型 1：概念插画（头图、比喻图）**

即梦版（中文，推荐国内用户）：
```
主体：[画面主角在做什么，1句话]
背景：[场景环境描述]
风格：扁平插画 / 3D卡通渲染 / 手绘水彩 / 赛博朋克
色调：[主色+辅色，如「蓝紫渐变背景，暖黄点缀」]
构图：[居中/对角线/三分法]，[横版16:9]
氛围：[温暖明亮 / 科技感 / 轻松幽默 / 专业冷静]
细节：[关键视觉元素]
```

Midjourney 版（英文）：
```
[Main subject description], [action/pose], [background setting],
[art style: flat vector / 3D render / watercolor / cyberpunk],
[color palette: e.g. blue-purple gradient, warm yellow accents],
[composition: centered, diagonal, rule of thirds],
[mood: warm, bright, techy, humorous, professional],
[detail elements], --ar 16:9 --v 6
```

**类型 2：截图示意（工具界面、App 界面）**

不需要 AI 生图。直接给出截图指引：
```
截图指引（💫💫 难度）：
1. 打开 [工具名]，做 [具体操作]
2. 截取 [具体区域]，注意 [敏感信息打码/保持整洁]
3. 如需标注：用 Canva 打开截图，加箭头+文字

快速标注方案（💫💫 难度）：
- 手机截图后用系统自带编辑（iOS标注/Android编辑）
- 加红色箭头或圆圈即可，不需要花哨
```

**类型 3：Before/After 对比**
```
即梦版：
主体：左右分屏对比，左边[前置状态描述]，右边[后置状态描述]
风格：照片级写实
色调：[统一色调]
构图：对称分屏，中间加转换箭头
细节：标注文字「Before」→「After」

Midjourney 版：
Split screen comparison, left side: [before state],
right side: [after state], photorealistic style,
labeled 'Before → After', --ar 16:9 --v 6
```

**类型 4：流程图/信息图**
```
💡 推荐方案：用 Canva 模板（💫💫 难度），不要用 AI 生图
- Canva 搜索「流程图」模板，替换文字即可
- 配色：主题色 + 白色背景

如坚持用 AI 生图（💫💫💫 难度）：
即梦版：
主体：[N]步流程横向排列，每步是圆角矩形卡片
风格：现代简约信息图，矢量风格
色调：白色背景 + [主题色]高亮，灰色辅助
构图：横向排列，箭头连接
```

**类型 5：封面图（社交媒体分享）**
```
方形封面（1:1，朋友圈/小红书 ⭐ 最重要）：
即梦版：
主体：大号文章标题文字居中，副标题小字在下方
背景：[与内容相关的插画，不要太复杂以免遮挡文字]
风格：扁平化海报风格
色调：[品牌色渐变背景]，白色文字
构图：标题居中，视觉元素四周环绕

横版封面（16:9，公众号头图）：
即梦版：
主体：文章核心概念视觉化 + 标题文字
风格：[与方形封面一致]
构图：主体偏右/偏下，标题偏上/偏左（留出公众号裁剪空间）
```

### D. 封面图专项（必出）

为每篇文章生成至少 2 版封面方案：

```
## 📱 社交媒体封面

### 方案一：文字为主（💫💫 难度，Canva）
模板：Canva → 搜索「公众号封面」
尺寸：900×383（公众号）/ 1080×1080（朋友圈）
内容：文章标题 + 一句话卖点 + 博主名「技趣星球」
配色建议：[基于文章主题推荐]

### 方案二：插画为主（💫💫💫 难度，即梦）
即梦提示词：
[完整提示词]
```

### E. 配色与风格指南

```
「技趣星球」品牌色板：
- 主色：#3B82F6（科技蓝）
- 辅色：#8B5CF6（创意紫）
- 强调色：#F59E0B（活力橙）
- 背景色：#0F172A（深色模式） / #F8FAFC（浅色模式）

文章配图配色建议：
- 技术/工具类 → 蓝 + 白（干净、专业）
- 创意/设计类 → 紫 + 粉（有趣、艺术）
- 实操/教程类 → 橙 + 绿（活力、行动感）
- 警示/边界类 → 红 + 灰（严肃、提醒）

字体建议：
- 标题：粗体无衬线（思源黑体 / 苹方）
- 正文：常规无衬线
- 封面文字：粗体 + 描边或投影（确保在缩略图下可读）
```

### F. 配图自查清单

```
生成每张 AI 图片后，检查：
□ 缩略图下文字是否可读？（50%缩放测试）
□ 主体是否突出？背景是否太杂？
□ 色彩是否统一？（不超过4个主色）
□ 是否适合目标平台？（公众号会裁切，小红书要正方形）
□ AI 生成的人脸/手部是否有明显畸形？（如有，换一张）
□ 文字内容是否在图片安全区内？
```

## 特殊场景处理

### 技术截图类文章
```
优先用真实截图（💫💫），不用 AI 生成
截图规范：
- 分辨率：至少 1200px 宽
- 敏感信息：账号、密码、API Key 必须打码
- 标注：红色箭头 / 圆圈 + 简短文字
- 风格：统一标注风格（颜色、字号、箭头样式）
```

### 工具推荐类文章
```
用「LOGO + 名称 + 一句话」卡片组（💫💫💫 难度）
即梦提示词模板：
[N]张卡片水平排列，每张卡片包含：[工具名]大号文字、小LOGO图标、
颜色与工具主题色呼应。现代简约信息图风格，白色背景，--ar 16:9
```

### 前后对比类文章
```
用真实操作截图做对比（💫💫 难度）
1. 看到原始状态 → 截图
2. 用 AI 处理 → 截图结果
3. 两张拼在一起 → Canva 左右对比模板
标注：Before / After，重点变化处加红色箭头
```

## 注意事项

- 优先输出 💫-💫💫 难度的方案（复制即用或 Canva 模板）
- 图片提示词要具体，避免「一张好看的图」这种模糊描述
- 封面是最高优先级——这是社交传播的第一视觉
- 稳定排版装饰为必选项，优先使用 Markdown 引用块、表格、短标签；不要使用字符边框
- 如果文章已存在于 `content/posts/`，将配图方案追加到文章末尾
- AI 生图可能需要多次尝试才能获得满意结果，每次给出 2-3 个变体建议

## 图片存放规范（重要）

每篇文章使用带序号前缀的独立文件夹，资源隔离：

```
content/posts/
└── [NN-slug]/           # 文章独立文件夹，NN=两位序号，slug=文章URL标识
    ├── index.md         # 文章正文
    └── images/          # 该文章专属图片目录
        ├── header.png
        ├── scene1-xxx.png
        └── ...
```

目录命名规则：
- 新文章默认使用 `NN-slug`，例如 `03-ai-one-person-company-reality`
- `NN` 按内容计划/发布时间顺序递增，使用两位数字
- 历史文章如果没有编号，不主动重命名；只有用户明确要求时再迁移旧目录和引用

### 文件命名规范

| 图片类型 | 推荐文件名 | 说明 |
|---------|-----------|------|
| 头图/封面 | `header.png` | 每篇文章固定命名 |
| 场景配图 | `scene1-[关键词].png` | 按场景序号+关键词 |
| 速览/总览 | `overview.png` 或 `scenes-overview.png` | 全局信息图 |
| Before/After | `before-after-[主题].png` | 对比图 |
| 工具截图 | `tool-[工具名].png` | 工具界面截图 |
| 社媒封面 | `cover-square.png` / `cover-wide.png` | 1:1 / 16:9 |

### 文章内引用路径

文章 `index.md` 内引用图片统一用相对路径：

```markdown
![描述](./images/header.png)
```

### 生成配图方案时的输出格式

每处配图方案必须包含存放路径提示：

```
📁 保存路径：content/posts/[NN-文章slug]/images/[文件名].png
```

例如：
```
📁 保存路径：content/posts/01-ai-for-ordinary-people/images/scene2-webpage.png
```

### 已有图片的处理原则

- 图片已生成并放入 `images/` 目录后，**立即去除文章中对应的提示词 blockquote**
- 只保留 `![alt](./images/xxx.png)` 引用行
- 未生成的图片保留提示词，方便后续制作
- 配图制作清单（`## 📸 配图制作清单`）属于内部工作文档，**发布前必须删除**
